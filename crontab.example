# App数据监控系统定时任务示例
# 请根据实际部署环境调整路径和时间

# 每日凌晨2点执行数据采集任务
0 2 * * * cd /path/to/app_data_monitor && python manage.py run_daily_task >> /var/log/app_monitor/daily_task.log 2>&1

# 每周一上午9点发送周报汇总（可选）
0 9 * * 1 cd /path/to/app_data_monitor && python manage.py run_daily_task --generate-weekly-report >> /var/log/app_monitor/weekly_report.log 2>&1

# 每小时检查一次系统健康状态（可选）
0 * * * * cd /path/to/app_data_monitor && python manage.py health_check >> /var/log/app_monitor/health_check.log 2>&1

# 每天清理30天前的日志和临时数据（可选）
30 1 * * * cd /path/to/app_data_monitor && python manage.py cleanup_old_data --days 30 >> /var/log/app_monitor/cleanup.log 2>&1